name: image-provider

services:
  image-provider:
    container_name: image-provider
    image: jav0032/retina:image-provider-${VERSION}
    build:
      target: ${TARGET}
      args:
        NODE_PORT: ${NODE_PORT}
    environment:
      NODE_VERSION: ${VERSION}
      NODE_DEBUG: ${NODE_DEBUG}
      NODE_HOSTNAME: ${NODE_HOSTNAME}
      NODE_PORT: ${NODE_PORT}
      NODE_UPLOAD_DIR: ${NODE_UPLOAD_DIR}
      NODE_UPLOAD_SIZE: ${NODE_UPLOAD_SIZE}
      NODE_REDOC_HOSTNAME: ${NODE_REDOC_HOSTNAME}
      NODE_REDOC_PORT: ${NODE_REDOC_PORT}
    ports:
      - ${NODE_PORT}:${NODE_PORT}
    restart: unless-stopped
    volumes:
      - ${NODE_UPLOAD_DIR}:${NODE_UPLOAD_DIR}

  redoc:
    container_name: redoc
    image: redocly/redoc:v2.1.4
    environment:
      SPEC_URL: 'http://${NODE_HOSTNAME}:${NODE_PORT}/docs'
      PAGE_TITLE: 'ReDoc - Image Provider'
    ports:
      - ${NODE_REDOC_PORT}:80
    restart: unless-stopped
    depends_on:
      image-provider:
        condition: service_healthy
